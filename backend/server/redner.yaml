services:
  - type: web
    name: memorizethat-backend
    env: node
    buildCommand: cd ../.. && npm install && cd backend && npm install
    startCommand: cd ../.. && cd backend && node server/server.js
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: JWT_SECRET
        sync: false